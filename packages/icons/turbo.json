{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        "dist/**"
      ],
      "inputs": [
        "build.ts",
        "raw/*.svg"
      ],
      "dependsOn": [
        "^build"
      ]
    }
  }
}