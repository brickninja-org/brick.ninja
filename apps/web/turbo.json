{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "outputs": [
        ".next/**",
        "!.next/cache/**"
      ],
      "env": ["SKIP_LINT"]
    },
    "dev": {
      "dependsOn": [
        "^build",
        "@brickninja-org/database#prisma:migrate"
      ]
    },
    "lint": {}
  }
}